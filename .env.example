# Neon Postgres database URLs
# Local dev: use your staging database URL (get from Neon dashboard or 'pnpm netlify env:pull')
# Production/staging databases are set per deploy context in Netlify dashboard
NETLIFY_DATABASE_URL=postgresql://user:password@host.neon.tech/dbname?sslmode=require

# Identity database URL — always used for users table (shared across environments)
# Set this to your production database URL in all environments
NETLIFY_DATABASE_URL_IDENTITY=postgresql://user:password@prod-host.neon.tech/dbname?sslmode=require

# Resend email service: get from resend.com dashboard
RESEND_API_KEY=re_xxxxxxxxxxxx
# Public city configuration: set in Netlify env vars dashboard
PUBLIC_CITY_NAME=Pittsburgh
PUBLIC_CITY_DOMAIN=pittsburgh.comedyconnector.app
PUBLIC_SITE_URL=https://pittsburgh.comedyconnector.app
# Deploy context label — controls the environment banner shown to users
# Set to "dev" locally; Netlify injects the correct value per context via netlify.toml
PUBLIC_DEPLOY_CONTEXT=dev
